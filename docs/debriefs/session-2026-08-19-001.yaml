# ================================================================
# GENERIC SESSION DEBRIEF — HQ
# Generated by Standard Project Kit (skill standard-project-kit)
# ================================================================

runbook:
  name: Session Debrief
  version: "2.0"
  trigger:
    phrases: ["run session debrief"]

project_config:
  project_name: "HQ"
  context_file: null
  framework_terms: null
  id_prefixes:
    architectural_decision: "AD-"
    open_question: "OQ-"
  output_location: "docs/debriefs/"

output_schema:
  session_id: "session-2026-08-19-001"
  date: "2026-08-19"
  status: "confirmed"

  summary:
    purpose: "Adopt the Standard Project Kit, record it in the master handoff, and verify HQ live."
    outcomes:
      - "HQ repo /root/hq confirmed in sync with origin/main at fc53fda1 (0 commits behind)."
      - "Recorded the Standard Project Kit adoption as a dated entry at the top of docs/KIMI-HANDOFF.md (the master cross-project log)."
      - "Verified live: hq.giveabit.io returns HTTP 200."
      - "Recorded the first machine-readable session debrief for HQ."

  architectural_decisions:
    - id: "AD-001"
      decision: "Treat /root/hq on THOR as the live/ops + handoff home for HQ and the master cross-project handoff log. M3/M4 clones are working copies synced via git push/pull only."
      rationale: "Consistent with the Standard Project Kit rule across all repos. HQ is also the hub of the ecosystem (ops glass, master handoff log), so it is the canonical place for cross-project rollouts."
      status: "agreed"

  open_questions:
    - id: "OQ-001"
      question: "Should per-project session debriefs also be mirrored into the HQ master log, or stay only in each repo's docs/debriefs/?"
      context: "HQ's KIMI-HANDOFF.md is the master cross-project log. Individual repo debriefs live in their own docs/debriefs/. Mirroring every debrief here would clutter the master log; not mirroring keeps it as the operations log."
      priority: "low"
      status: "open"

  session_notes:
    deprecated_items: []
    corrections_to_prior_sessions: []
    other:
      - "Trial run of the Standard Project Kit on HQ. First debrief for this repo."
      - "HQ is the ecosystem hub: live pipes (api.satohash.io/metrics.json, status pinger), password gate + browser Vault. No invoice keys in git."
