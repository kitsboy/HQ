{
  "name": "giveabit-hq",
  "private": true,
  "version": "3.20.0",
  "description": "Give A Bit HQ — navigation fix, Safe Harbour v2, file inventory, roadmap",
  "scripts": {
    "stamp": "node scripts/stamp-version.mjs",
    "verify-version": "node scripts/stamp-version.mjs",
    "build": "npm run stamp && node scripts/build-public.mjs",
    "deploy": "npm run build && wrangler pages deploy ./public --project-name=giveabit-hq",
    "preview": "python3 -m http.server 8765 --directory public"
  },
  "devDependencies": {
    "wrangler": "^4.112.0"
  }
}